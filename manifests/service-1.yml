apiVersion: v1
kind: Service
metadata:
    name: "jayaks1"
    labels:
        app: "jayaks1"
spec:
    type: ClusterIP
    ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
    selector:
        app: "jayaks1"